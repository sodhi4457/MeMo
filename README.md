# MeMo — Step A (Data Synthesis)

Local implementation of Step A from the MEMO paper (Memory as a Model).
This step turns a corpus of documents into a reflection Q&A dataset that
the Memory Model is later fine-tuned on.

See `Memo-Complete-Breakdown.md` for the full paper walkthrough and
`TESTING.md` for setup + verification steps.
