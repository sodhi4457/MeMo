"""MEMO Step B: Memory Model supervised fine-tuning.

This package is intentionally independent of `memo` (Step A). It only
consumes the JSONL reflection dataset produced by Step A — no imports
from the Step A package, so the two pieces can ship and break separately.
"""

__version__ = "0.1.0"
